# Phase VIII M4 Close — Codex Review (G4b)

**Reviewer:** Codex gpt-5.5 xhigh
**Date:** 2026-05-01T01:15:34Z
**Diff range:** 666c9a29..b4dd828a
**Files reviewed:** experiments/adaptive_beta/baselines.py; experiments/adaptive_beta/schedules.py; results/adaptive_beta/tab_six_games/M4_W1.A_handoff.md; results/adaptive_beta/tab_six_games/M4_W1.B_handoff.md; results/adaptive_beta/tab_six_games/M4_W2.A_handoff.md; results/adaptive_beta/tab_six_games/M4_W2.B_handoff.md; results/adaptive_beta/tab_six_games/M4_W2.C_handoff.md; results/adaptive_beta/tab_six_games/M4_W2.D_handoff.md; tests/adaptive_beta/tab_six_games/test_baselines.py; tests/adaptive_beta/tab_six_games/test_beta0_collapse_preserved.py; tests/adaptive_beta/tab_six_games/test_clipping_bounds.py; tests/adaptive_beta/tab_six_games/test_contraction_reward_finite.py; tests/adaptive_beta/tab_six_games/test_contraction_ucb_arm_accounting.py; tests/adaptive_beta/tab_six_games/test_contraction_ucb_schedule.py; tests/adaptive_beta/tab_six_games/test_hand_adaptive_schedule.py; tests/adaptive_beta/tab_six_games/test_oracle_beta_schedule.py; tests/adaptive_beta/tab_six_games/test_return_ucb_schedule.py; tests/adaptive_beta/tab_six_games/test_return_ucb_standardisation.py

## Verdict: PASS

## Findings

### BLOCKER
None.

### MAJOR
None.

### MINOR
None.

### NIT
None.

## Per-focus-area assessment

- Focus 1 (operator-touch): CLEAN — `git diff 666c9a29..b4dd828a -- src/lse_rl/operator/tab_operator.py mushroom-rl-dev/mushroom_rl/algorithms/value/dp/safe_weighted_common.py` is empty.
- Focus 2 (single-update-path): CLEAN — `git diff 666c9a29..b4dd828a -- experiments/adaptive_beta/agents.py` is empty; the existing call at `agents.py:239` still passes only `divergence_event`.
- Focus 3 (beta0 guard): CLEAN — the `_step_update` `raise AssertionError` guard remains untouched at `agents.py:338-345`; `test_beta0_collapse_preserved.py` parametrizes all 12 `ALL_METHOD_IDS`, including ContractionUCB arm 3 beta 0.0, and verifies beta 0.0 produces the classical Bellman target bit-identically.
- Focus 4 (epsilon/log discipline): CLEAN — `ContractionUCBBetaSchedule` uses `epsilon_floor=1e-8` and `np.log(prev + eps) - np.log(smoothed_R + eps)` at `schedules.py:831,860`; `metrics.contraction_reward` uses `EPSILON=1e-8` and `np.log` at `metrics.py:35,78`. Executable-code searches found no `np.log1p`, `log1p(`, `np.expm1`, or `expm1(` path.
- Focus 5 (UCB warm-start): CLEAN — default grid is `(-2.0, -1.0, -0.5, 0.0, 0.5, 1.0, 2.0)` at `schedules.py:65-67`; `_select_next_arm` forces episodes 0..6 then uses `argmax(mu_hat_std + c * sqrt(2 log N / N_j))` from episode 7 with numpy lowest-index tie-break at `schedules.py:720-751`.
- Focus 6 (Welford standardisation): CLEAN — Welford updates mean/M2 correctly and documents population std (`ddof=0`) at `schedules.py:501-519`; per-arm stats and per-arm standardized reward means are used for UCB scoring at `schedules.py:653-661,768-777`. ContractionUCB uses `c=1.0`; ReturnUCB uses `sqrt(2)` and documents why standardisation is required for matrix-game returns at `schedules.py:811-813,868-872,879`.
- Focus 7 (Oracle info discipline): CLEAN — Oracle raises on missing `episode_info`, missing `"regime"`, and unknown regimes at `schedules.py:558-576`; beta is exactly the lookup from `info["regime"]` and ignores smoothed advantage.
- Focus 8 (HandAdaptive defaults): CLEAN — class-level defaults are pinned at `schedules.py:594-597` as beta0 1.0, A_scale 0.1, lambda_smooth 1.0, matching the factory defaults.
- Focus 9 (baseline state extraction): CLEAN — public state/action/next-state extraction routes through `_as_int`, implemented as `int(np.asarray(x).flat[0])` at `baselines.py:53-60`; searches found no direct `int(state)`, `int(action)`, or `int(next_state)` casts.
- Focus 10 (result root): CLEAN — no diff-added code hard-codes `results/weighted_lse_dp`; Phase8RunRoster default-root references remain outside this diff and point to `results/adaptive_beta/tab_six_games`.
- Focus 11 (M4 W2.B auto-fix): CLEAN — `_pull_counts[_current_arm_idx] += 1` fires at the start of every `_compute_next_beta` call before optional reward attribution at `schedules.py:779-793`; `_record_arm_reward` no longer increments pull counts, so there is no double count and the just-deployed arm receives the count.
- Focus 12 (allow-list keys): CLEAN — all four new schedules have `_ALLOWED_KEYS` entries at `schedules.py:122-127`; `regime_to_beta` and `arm_grid` bypass `_resolved_hparams` float coercion through explicit constructor extraction at `schedules.py:947-993`.
- Focus 13 (test-set size): CLEAN — the ten M4-added test files collect exactly 87 tests and pass as `87 passed`; `tests/adaptive_beta/tab_six_games` passes as `144 passed, 1 skipped`. Exact `b4dd828a` full-suite run required a temp snapshot with `.venv` symlink plus OpenMP limits and passed as `1612 passed, 2 skipped`; the second skip is a pre-existing Phase V planning skip unrelated to M4.
- Focus 14 (ALL_METHOD_IDS): CLEAN — the four new snake_case method constants are present and included once in `ALL_METHOD_IDS` at `schedules.py:42-60`.
- Focus 15 (Protocol surface): CLEAN — `BetaSchedule.update_after_episode` now accepts optional `episode_info`, `bellman_residual`, and `episode_return` kwargs with default `None` at `schedules.py:175-184`; legacy concrete schedules still inherit the base update path and ignore these kwargs.

## Summary

No operator or single-TD-update-path drift was found in `666c9a29..b4dd828a`. The new schedules preserve beta-zero collapse, UCB warm-start/accounting, per-arm standardisation, oracle regime discipline, result-root hygiene, and state-extraction invariants. The only verification caveat is environmental: unconstrained full-suite subprocess tests hit OpenMP shared-memory failures in this sandbox, but the exact reviewed snapshot passes under constrained OpenMP settings and the M4-added tests pass directly.
