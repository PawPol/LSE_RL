# Phase VIII M2 W2.B handoff - test_potential.py

## Summary

- Added `tests/adaptive_beta/strategic_games/test_potential.py` with the
  ten requested test functions covering potential-game construction,
  payoff correctness, pinned `compute_potential(...)` values,
  better-reply potential monotonicity, canonical sign, inertia penalty,
  switching-payoff cadence, horizon termination, `info["regime"]`, and
  deterministic same-seed rollouts.
- Kept the dispatch boundary intact: no edits to `test_registry.py`, `src/`,
  `mushroom-rl-dev/`, or `experiments/`.
- Verified the new file with the required venv pytest invocation.

## Artifacts

- `tests/adaptive_beta/strategic_games/test_potential.py`
- `results/adaptive_beta/tab_six_games/M2_W2.B_handoff.md`

## Verification evidence

Required command:

```bash
.venv/bin/python -m pytest tests/adaptive_beta/strategic_games/test_potential.py -q
```

Result:

```text
.........................                                                [100%]
```

Timed repeat:

```bash
/usr/bin/time -p .venv/bin/python -m pytest tests/adaptive_beta/strategic_games/test_potential.py -q
```

Result:

```text
.........................                                                [100%]
real 1.62
user 1.41
sys 0.17
```

Collected pytest cases: 25. Passed: 25. Failed: 0.

## Open questions

- None.
