# Phase VIII M1 W1.B handoff — Phase8RunRoster

## Summary

- Implemented `Phase8RunRoster` per spec §8.2 in
  `experiments/adaptive_beta/tab_six_games/manifests.py` with a 12-field
  `RosterRow` dataclass, 7-status enum, JSONL atomic write/read, strict
  state-machine, duplicate `cell_id` detection, and disk reconciliation.
- Default `base_path = Path("results/adaptive_beta/tab_six_games")`,
  satisfying the spec §13.6 result-root regression contract and
  lessons.md default-root drift rule.
- Module is stdlib-only (`json`, `os`, `pathlib`, `dataclasses`,
  `typing`); no numpy / pandas / cross-experiment imports.

## Artifacts

- `experiments/adaptive_beta/tab_six_games/__init__.py` (empty package
  marker, new)
- `experiments/adaptive_beta/tab_six_games/manifests.py` (new, 22 KiB)
- `results/adaptive_beta/tab_six_games/W1.B_handoff.md` (this memo)

## Verification evidence

Smoke script `/tmp/w1b_smoke.py` (executed with
`.venv/bin/python /tmp/w1b_smoke.py` from the repo root after
exporting `PYTHONPATH=<repo-root>` because `experiments/` is a PEP 420
namespace package not on `sys.path` by default; the smoke import line
from the brief itself runs cleanly when invoked with cwd at the repo
root). The script was deleted after capture.

```
=== check 1: smoke import ===
OK {'total': 0, 'by_status': {}, 'by_method': {}, 'by_game_subcase': {}}
default base_path: results/adaptive_beta/tab_six_games

=== check 2: roundtrip ===
summary_before: {'total': 2, 'by_status': {'pending': 2}, 'by_method': {'vanilla': 1, 'fixed_beta_-1': 1}, 'by_game_subcase': {'matching_pennies/canonical': 1, 'shapley/canonical': 1}}
summary_after: {'total': 2, 'by_status': {'pending': 2}, 'by_method': {'vanilla': 1, 'fixed_beta_-1': 1}, 'by_game_subcase': {'matching_pennies/canonical': 1, 'shapley/canonical': 1}}
OK roundtrip

=== check 3: duplicate cell_id detection ===
got expected ValueError: duplicate cell_id in roster: 'matching_pennies/canonical/vanilla/0' (already held by run_id='run_a')
OK duplicate detection

=== check 4: state machine ===
after pending->running: running
got expected ValueError on running->pending: invalid status transition for run_id='run_a': 'running' -> 'pending' (allowed targets: ['completed', 'diverged', 'failed', 'stopped-by-gate'])
OK state machine

ALL FOUR SMOKE CHECKS PASSED
```

Brief's exact one-liner re-run from repo root:

```
$ cd <repo-root> && .venv/bin/python -c "from experiments.adaptive_beta.tab_six_games.manifests import Phase8RunRoster, RosterRow, SCHEMA_VERSION; r = Phase8RunRoster(); print('OK', r.summarize())"
OK {'total': 0, 'by_status': {}, 'by_method': {}, 'by_game_subcase': {}}
```

## Open questions

- none.

## Compliance attestations

- No edits to `src/lse_rl/operator/tab_operator.py`.
- No edits to
  `mushroom-rl-dev/mushroom_rl/algorithms/value/dp/safe_weighted_common.py`.
- No edits to `experiments/adaptive_beta/agents.py`,
  `experiments/adaptive_beta/schedules.py`, or any
  `experiments/weighted_lse_dp/common/*.py` precedent file.
- No edits under `tests/`.
- No package installs; only the existing `.venv` was used.
- No use of `expm1` or `log1p` (lessons.md #27, cross-checked).
