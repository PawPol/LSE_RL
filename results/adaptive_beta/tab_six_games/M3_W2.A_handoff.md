# M3 W2.A handoff — inertia adversary tests + registry count

## Summary

- Added focused tests for the Phase VIII M3 `InertiaOpponent` contract:
  registry presence, high-inertia stickiness, zero-inertia uniformity,
  lambda validation, determinism, integer action type, `info()` schema,
  and reset round-trip determinism.
- Updated `tests/adaptive_beta/strategic_games/test_registry.py` so the
  expected adversary set includes the three M3 adversaries:
  `inertia`, `convention_switching`, and `sign_switching_regime`.
- No edits were made to `src/`, `mushroom-rl-dev/`, or `experiments/`.

## Artifacts

- Created: `tests/adaptive_beta/strategic_games/test_inertia_adversary.py`
- Edited: `tests/adaptive_beta/strategic_games/test_registry.py`
- Created: `results/adaptive_beta/tab_six_games/M3_W2.A_handoff.md`

## Verification evidence

Exact requested command:

```bash
.venv/bin/python -m pytest tests/adaptive_beta/strategic_games/test_inertia_adversary.py tests/adaptive_beta/strategic_games/test_registry.py -q
```

Result:

```text
...................                                                      [100%]
```

Timed rerun with the same pytest target:

```text
real 1.64
user 1.46
sys 0.17
```

Registry-only confirmation:

```bash
.venv/bin/python -m pytest tests/adaptive_beta/strategic_games/test_registry.py -q
```

Result:

```text
........                                                                 [100%]
```

Inertia test file collection:

```text
tests/adaptive_beta/strategic_games/test_inertia_adversary.py: 11
```

The inertia file has 8 logical test functions; `test_lambda_validation`
is parameterized over 4 invalid values, so pytest collects 11 items.

## Open questions

- None.
